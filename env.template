# College Compass — copy this file to .env and fill in your keys. Never commit .env.

# College Scorecard API (school discovery) — FREE key from https://api.data.gov/signup/
# Without this key, discovery falls back to a shared demo key that is heavily rate-limited.
SCORECARD_API_KEY=

# Choose one LLM backend for the Pro path (leave both blank for Free/Ollama path)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
