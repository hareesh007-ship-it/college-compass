# Pro path (Option B) — copy to .env and fill in. Never commit .env.

# College Scorecard (school discovery) — FREE key from https://api.data.gov/signup/
# See docs/DELIVERY_CHANNEL.md §8 for setup, troubleshooting, and what happens without a key.
# Loaded automatically by scripts/run.py via load_env_file().
SCORECARD_API_KEY=

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
